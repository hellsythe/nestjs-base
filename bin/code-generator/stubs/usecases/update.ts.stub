import { Inject } from '@nestjs/common';
import { {{pascalCase}}Model } from '@entities/{{kebabCase}}.model';
import { I{{pascalCase}}Repository } from '@usecases/{{kebabCase}}/repository.interface';
import { Update{{pascalCase}}DtoInterface } from './dtos/update.dto';

export class Update{{pascalCase}} {
  constructor(
    @Inject('{{modelCamel}}Repository')
    private {{modelCamel}}Repository: I{{pascalCase}}Repository,
  ) {}

  async execute(
    query: any,
    {{modelCamel}}: Update{{pascalCase}}DtoInterface,
  ): Promise<{{pascalCase}}Model> {
    const current{{pascalCase}} = await this.{{modelCamel}}Repository.findOne(query);
    const new{{pascalCase}} = { ...current{{pascalCase}}, ...{{modelCamel}} };

    await this.{{modelCamel}}Repository.update(query, new{{pascalCase}});

    return this.{{modelCamel}}Repository.findOne(query);
  }
}
