import { {{pascalCase}}Model } from '@entities/{{kebabCase}}.model';
import { createFake{{pascalCase}} } from '@test/factory/{{kebabCase}}.factory';
import { Create{{pascalCase}} } from './create';
import { TestBed } from '@automock/jest';
import { I{{pascalCase}}Repository } from '@usecases/{{kebabCase}}/repository.interface';

describe('create{{pascalCase}}', () => {
  let create{{pascalCase}}: Create{{pascalCase}};
  let {{modelCamel}}Repository: jest.Mocked<I{{pascalCase}}Repository>;

  beforeAll(async () => {
    const { unit, unitRef } = TestBed.create(Create{{pascalCase}}).compile();
    create{{pascalCase}} = unit;
    {{modelCamel}}Repository = unitRef.get('{{modelCamel}}Repository');
  });

  test('create{{pascalCase}}', async () => {
    const {{modelCamel}}Fake: {{pascalCase}}Model = createFake{{pascalCase}}();
    {{modelCamel}}Repository.create.mockResolvedValueOnce({{modelCamel}}Fake);

    await create{{pascalCase}}.execute({{modelCamel}}Fake);

    expect({{modelCamel}}Repository.create).toHaveBeenCalledWith({{modelCamel}}Fake);
  });
});
