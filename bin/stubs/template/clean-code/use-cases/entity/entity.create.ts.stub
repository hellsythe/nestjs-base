import { Inject } from '@nestjs/common';
import { {{pascalCase}}Model } from '@entities/{{kebabCase}}.model';
import { I{{pascalCase}}Repository } from '@usecases/{{kebabCase}}/repository.interface';
import { Create{{pascalCase}}DtoInterface } from './dtos/create.dto';

export class Create{{pascalCase}} {
  constructor(
    @Inject('{{camelCase}}Repository')
    private {{camelCase}}Repository: I{{pascalCase}}Repository,
  ) {}

  async execute({{camelCase}}: Create{{pascalCase}}DtoInterface): Promise<{{pascalCase}}Model> {
    return await this.{{camelCase}}Repository.create({{camelCase}});
  }
}
