import { {{pascalCase}}Model } from '@entities/{{kebabCase}}.model';
import { createFake{{pascalCase}} } from '@test/factory/{{kebabCase}}.factory';
import { Create{{pascalCase}} } from './create.{{kebabCase}}';
import { TestBed } from '@automock/jest';
import { I{{pascalCase}}Repository } from '@usecases/{{kebabCase}}/{{kebabCase}}.repository.interface';

describe('create{{pascalCase}}', () => {
  let create{{pascalCase}}: Create{{pascalCase}};
  let {{camelCase}}Repository: jest.Mocked<I{{pascalCase}}Repository>;

  beforeAll(async () => {
    const { unit, unitRef } = TestBed.create(Create{{pascalCase}}).compile();
    create{{pascalCase}} = unit;
    {{camelCase}}Repository = unitRef.get('{{camelCase}}Repository');
  });

  test('create{{pascalCase}}', async () => {
    const {{camelCase}}Fake: {{pascalCase}}Model = createFake{{pascalCase}}();
    {{camelCase}}Repository.create.mockResolvedValueOnce({{camelCase}}Fake);

    await create{{pascalCase}}.execute({{camelCase}}Fake);

    expect({{camelCase}}Repository.create).toHaveBeenCalledWith({{camelCase}}Fake);
  });
});
